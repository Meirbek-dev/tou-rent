# GAUNTLET — журнал прогона на производственную готовность

Формат: `G-NNN | круг | измерение | важность | доказательство | статус`.
Статусы: `open` → `fixed` (перепроверено свежим критиком) / `wontfix-logged`
(вне области, нужен человек) / `blocked` (три стратегии не помогли).

Область и границы — `.claude/commands/gauntlet.md` (§1 цель, §2 метрика,
§3 границы). Критик и исполнитель — всегда разные контексты: находку
закрывает не тот, кто ее чинил.

## Круг 1

| ID    | Измерение     | Важн.  | Находка                                                                                                                                                                                      | Доказательство                                                                                                                                                        | Статус                             |
| ----- | ------------- | ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------- |
| G-000 | dev-стенд     | S1     | `api-migrate` падал с exit 101: макросы sqlx проверялись по живой БД, а контейнер компилируется до наката миграций                                                                           | `podman compose ... --profile api up -d` → 40 ошибок «column/function does not exist» + E0308                                                                         | fixed                              |
| G-001 | frontend/UX   | S2     | Пять списков молча теряют признак `truncated`: пользователь видит обрезанную выборку без единого намека. Ключ `list_truncated` уже есть в трех локалях, у соседних панелей — рабочий образец | `lib/public-records.ts:28` возвращает `.items`, отбрасывая конверт страницы; то же в `lib/contracts.ts`, `lib/ledger.ts`, `lib/obligations.ts`, `lib/publications.ts` | fixed                              |
| G-002 | i18n          | S3     | Английские строки доступности в `components/ui/*` минуют Paraglide; «Toggle Sidebar» виден в русском интерфейсе на каждой странице кабинета                                                  | `get_page_text` любой страницы `/app/*` содержит «Toggle Sidebar»; `ui/sidebar.tsx:287,290`                                                                           | fixed                              |
| G-003 | PDF/документы | S3     | Внутренний маркер `TODO-ENGINEER` печатается в пользовательских PDF (договор, протоколы)                                                                                                     | `templates/contract.typ:75` — `#text(...)[TODO-ENGINEER: ...]` внутри блока отрисовки                                                                                 | fixed                              |
| G-004 | web/статика   | S3     | Нет `robots.txt` — публичный портал не управляет обходом краулеров                                                                                                                           | `curl -o /dev/null -w %{http_code} /robots.txt` → 404                                                                                                                 | fixed                              |
| G-005 | API/домен     | S2     | `POST /rates/preview` с большой площадью ронял соединение без ответа: умножение `Decimal` паникует, паника в обработчике рвет сокет (нарушение А.4)                                          | `curl … -d '{"area_m2":"9999999999999999999999999999"}'` → `curl: (52) Empty reply from server`; порог между 5e24 и 6e24                                              | fixed                              |
| G-006 | БД/аудит      | S2     | `TRUNCATE` не вызывает построчных триггеров: весь журнал аудита стирался одним оператором, после чего `audit.verify_chain()` объявлял пустую цепочку целой                                   | `TRUNCATE audit.log` → `TRUNCATE TABLE`, затем `verify_chain()` → `t`                                                                                                 | fixed                              |
| G-007 | БД/домен      | S2     | `trading_at` считался от `CURRENT_TIMESTAMP`, а не от `core.now()`: при сдвинутых часах стенда дата торгов расходилась с остальными сроками тендера (ADR-0005)                               | в скретч-БД при сдвиге 30 дней: старый код → `2026-09-04`, новый → `2026-10-06`, расхождение 32 дня                                                                   | fixed                              |
| G-008 | frontend/UX   | S3     | Длинное наименование тендера вылезало за карточку и срезалось `overflow-hidden` на узком экране                                                                                              | на 375px: `scrollWidth` 358 при `clientWidth` 309, `overflow-wrap: normal`                                                                                            | fixed                              |
| G-009 | гейты         | S3     | `vp run gates --all` был красным по всему дереву: условный `test.skip` без токена G2                                                                                                         | `FAIL G2 … accessibility.spec.ts:99`                                                                                                                                  | fixed                              |
| G-010 | доступность   | **S2** | **Гейт G17 (NFR-04, NFR-15, WCAG 2.1 AA) красный: 15 тестов из 17.** Причина — `--primary` задан одним и тем же значением в обеих темах, а как текст он не проходит контраст ни в одной      | прогон против прод-сборки, обе темы; замеры ниже                                                                                                                      | **wontfix-logged (нужен человек)** |

### G-010 подробно: контраст сломан у токена, а не у страниц

Прогон `bun run --cwd apps/e2e e2e -- --grep доступность` **против
прод-сборки** (как в CI, а не против дев-сервера): 15 из 17 тестов красные,
нарушение всегда одно — `color-contrast (serious)`.

Замеры (canvas-разрешение цвета, порог AA для мелкого текста — 4.5:1):

| Элемент                         | Светлая | Темная | Нужно |
| ------------------------------- | ------- | ------ | ----- |
| ссылка `text-primary` 14px      | 4.38    | 3.79   | 4.5   |
| номер шага `text-primary` 14px  | —       | 3.50   | 4.5   |
| бейдж `info`                    | 4.17    | 3.30   | 4.5   |
| бейдж `success` (для сравнения) | 4.86    | 7.68   | 4.5   |

Причина видна в `styles/globals.css`: `--primary: oklch(0.56 0.188 259.8145)`
задан **одинаково** в светлой (строка 61) и темной (строка 105) теме, тогда
как `--sidebar-primary` для темной осветлен (0.546 → 0.623). У `success`,
`warning` и `destructive` в `ui/badge.tsx` текстовый цвет подобран отдельно
для каждой темы, с указанием отношения контраста прямо в комментарии —
`info` и голый `text-primary` в этот проход просто не попали.

**Почему не исправлено здесь.** `--primary` работает и заливкой
(`bg-primary` у кнопок с белой подписью), и текстом (22 употребления
`text-primary`). Осветлить его ради текста в темной теме — значит ухудшить
контраст белой подписи на кнопке. Развести заливку и текст в разные токены
можно, но это правка фирменного цвета по всему интерфейсу, а решение о
фирменном цвете принимает не агент (§ 3 регламента: границы).

**Что предлагается инженеру.** Развести токены, оставив заливку как есть:
`--primary` остается брендовым для `bg-primary`, добавляется текстовый
вариант (в светлой теме темнее, в темной — светлее), и на него переводятся
22 употребления `text-primary` плюс вариант `info` в `ui/badge.tsx`.
Проверка — тот же прогон, он же и гейт.

### Происшествие круга 1 (испорченные данные стенда)

Проверяя G-006, я выполнил `TRUNCATE` по живой базе стенда, а не по
одноразовой копии, как требует § 3 регламента. Первая редакция миграции
фильтровала триггеры по биту 8 (`DELETE`), а не 32 (`TRUNCATE`), поэтому
ни одного сторожа не создала — и четыре `TRUNCATE` прошли:

| Таблица                | Было | Стало |
| ---------------------- | ---- | ----- |
| `audit.log`            | ~90k | 0     |
| `core.bids`            | ?    | 0     |
| `core.ledger_entries`  | ?    | 0     |
| `core.journal_entries` | ?    | 0     |

Данные невосстановимы: цепочка аудита стенда стерта. Уцелели
`tenders` (154), `applications` (121), `users` (247), `contracts` (8) —
то есть стенд сейчас несогласован: договоры без проводок, заявки без
журнала. Прод и код не затронуты; потеряны только данные дев-стенда,
который по замыслу пересоздаваем.

Восстановление — пересборка стенда с нуля (том `pg_data` удаляется):

```
vp run stack:down && podman volume rm tou-rent-dev_pg_data && vp run stack:up
```

Сама находка G-006 после исправления бита проверена уже правильно —
в одноразовой `gauntlet_tr`, которая затем удалена.

### Снятые гипотезы (доказательство убило находку)

- Просроченный срок в плитке «Ближайшие сроки приема заявок» — не дефект:
  `deadlineUrgency` имеет состояние `overdue`, тон плитки берется от него,
  показ просроченного срока организатору намеренный (`lib/relative-time.ts:49`).
- «99+» и отсутствие пагинации в списке тендеров организатора — не потолок:
  `MAX_ROWS = 1000`, а в списке 99 реальных черновиков. Настоящая проблема
  рядом и в G-001 — не в самом потолке, а в том, что о нем молчат.
- «Два 404 в консоли на каждой странице» — не приложение: это пробы самого
  браузера (`/.well-known/appspecific/com.chrome.devtools.json` и иконка для
  экрана «Домой»). Все 250 ресурсов страницы отдаются 200.
- «Прод-сборка теряет `res/` и `favicon.ico`» — нет: оба импортируются как
  ассеты Vite (`?url`). `logo-blue.webp` (4442 Б) выше порога встраивания и
  лежит файлом, `logo-white.webp` (3012 Б) и favicon (366 Б) — ниже, и
  встроены как data-URI. Прод-сервер отдает главную 200.
- «PDF договора и протоколов отдают 500 из-за правки шаблонов» — нет:
  `NoSuchKey` в RustFS. Эти маршруты отдают **сохраненный** объект, а не
  собирают документ заново; объекты потерялись вместе с томом стенда.
  Правка шаблонов проверена иначе — поиском строк в собранном бинарнике.

### Вне области (нужен человек, зафиксировано)

- Коэффициенты Прил. 4 и МРП засеяны фиктивными `1.0000` / `9999` —
  нужен текст Правил (Q/A-010).
- Тела юридических форм в `crates/http/src/templates/*.typ` — формулировки
  приложений Правил (Q-004/005/014/017).
- Состав демо-комиссии (п. 9–11), внешние каналы уведомлений FR-1303 (T41).
- Казахские формы 11 ключей `rate_coef_*`: комментарий
  `components/rate-fields.tsx:27` прямо говорит, что «Копф» читается на kk
  иначе, чем на ru — нужен носитель языка, транслитерация наугад запрещена.
- `specs/BACKLOG.md`: T41 записан дважды (строки 415 и 502) — учетная запись,
  на код не влияет.

### Найдено критиками, не чинилось — очередь инженеру

По убыванию важности. Все подтверждено доказательством, все выходит за
границы § 3 (реквизиты, фирменный стиль, предметное решение) либо требует
правки, чью цену стоит взвесить человеку.

1. **G-010, контраст (S2)** — выше. Красный гейт NFR-15.
2. **Подключение к БД идет суперпользователем с `BYPASSRLS` (S2).**
   `DATABASE_URL` и в dev, и в prod указывает на `tou_rent`
   (`rolsuper=t, rolbypassrls=t`), а печать цен (INV-040) держится
   исключительно на `SET ROLE tou_rent_app` в `after_connect`
   (`crates/db/src/lib.rs:221`). Доказано: без `SET ROLE` те же ценовые
   предложения нераскрытого тендера читаются (3 строки), после — 0.
   Сейчас печать не течет, но она держится на одной строке кода вместо
   прав подключения. Лечится тем, что `tou_rent_app` получает LOGIN и
   пароль, `DATABASE_URL` переводится на него, а суперпользователь
   остается только для `api migrate`. Это правка реквизитов — § 3.
3. **`verify_chain()` не отличает «не подделано» от «нечего сверять» (S2).**
   На стенде это видно прямо сейчас: `audit.chain_checks` помнит
   `entries=4782`, в `audit.log` после происшествия 0 строк, а сверка
   отвечает `t`. Дешевый внутренний сигнал уже есть и не используется:
   `audit.chain_checks.entries` монотонна, и ее падение — это и есть
   событие. Внешний якорь последнего `row_hash` — Q-018.
4. **Нет границы паники в HTTP-слое (S3).** G-005 закрыл один случай,
   а не класс: `tower-http` собран без `catch-panic`, `CatchPanicLayer`
   нигде нет. Любая будущая паника в обработчике так же оборвет соединение
   вместо 500 problem+json.
5. **Ничто не ловит возврат `CURRENT_TIMESTAMP` в строку запроса (S3).**
   `clock_single_source.rs` проверяет умолчания колонок и тела функций
   `core.*`, гейт G2/SQL — только миграции. Ровно там, где жил G-007,
   проверки нет.
6. **Расхождение БД и хранилища ничем не сверяется (S3).** `has_pdf`/
   `has_file` в БД и объект в RustFS живут независимо; при расхождении
   пользователь видит кнопку «Документ», которая отдает 500 `internal`.
   Пропавший документ заслуживает типизированного 404, а не `internal`
   (А.5), и отдельно — сверки.
7. **Нет гейта на хардкод строк UI (S3).** `messages.test.ts` держит
   паритет каталогов, но не ищет литералы в разметке. G-002 нашли глазами,
   остаток (`ui/carousel.tsx`, `ui/pagination.tsx`, `ui/command.tsx`,
   `ui/questionnaire.tsx`) — там же и лежит, пока эти компоненты никто
   не импортирует.
8. **У расчета ставки нет границы правдоподобия (S3).** После G-005
   переполнение отбивается, но `area_m2 = 5e24` дает 200 и годовую плату
   7.49e28 ₸, а `1e-28` — 200 и гарантийный взнос 0. Разумные границы
   площади — предметный вопрос.
9. **`GET /commissions/active` отдает `user_id` членов комиссии (S3)**
   любому аутентифицированному пользователю. Если состав публичен по
   п. 17, из DTO стоит убрать внутренние идентификаторы.
10. **Авторизация проверяется после разбора тела запроса (S3).** Не дыра
    (корректное тело от участника получает 403), но неавторизованный
    получает 422 со схемой вместо 403.
11. **`deadline-chip` показывает число сроков без оглядки на потолок.**
    После G-001 признак `truncated` до значка не доходит: на потолке он
    скажет «1000», имея в виду «не меньше 1000».

## Круг 2

Семь критиков со свежим контекстом против живого стенда, затем восьмой -
по кабинетам (первый прогон фронтенда упёрся в блокер стенда, см. ниже).
Нумерация находок - по критику: RC (Rust), AC (контракт API), FE/FEC
(фронтенд), I18 (локали), UW (незавершенное), SEC (доступ), DI (данные).

### Происшествие круга 2 (блокер стенда, не дефект кода)

Веб-сервер :3000 отдавал прод-сборку, поднятую **до** последней пересборки:
манифест ссылался на `index-DihtFd0V.js`, которого на диске уже не было, а
лежавший там `index-DpZekUqk.js` сервер не знал. Все динамические чанки
маршрутов отдавали 500/404, вход был физически недоступен, и первый
фронтенд-критик не смог проверить ни одного кабинета (30+ маршрутов из 45).
Публичные страницы при этом «работали» - из кеша браузера
(`transferSize: 0` при `encodedBodySize: 578880`).

Починка - перезапуск веба оркестратором (стенд принадлежит ему): после
рестарта манифест совпал с диском, `/auth/login` и чанки отдаются 200.
Аудит кабинетов перезапущен отдельным критиком. Дефект отсюда всё же
вырос - FE-1: приложение не умеет переживать такой рассинхрон.

### Находки круга 2

| ID             | Измерение     | Важн.  | Находка                                                                                                                                                                                                                   | Доказательство                                                                                                                                   | Статус                                                                    |
| -------------- | ------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------- |
| RC-1 / DI-7    | аудит         | **S1** | Мутации `core.objects`, `core.auctions`, `core.ledger_accounts` не писали ни одного audit-события; гейт G15 односторонний и такую таблицу не видит                                                                        | прогон create → update → delete объекта через API: `audit.log` 91 → 91, строк о таблице `core.objects` - 0                                       | fixed                                                                     |
| RC-2           | аудит         | **S1** | 46 триггеров `audit.record`, 3 `record_natural_key` и 4 заморозки `freeze_*` были ORIGIN: `SET session_replication_role='replica'` + UPDATE переписывал подписанные условия договора, не оставив следа                    | в одноразовой БД: UPDATE/INSERT/DELETE под replica, `audit_rows` не выросли, `monthly_rate` 50000 → 1                                            | fixed                                                                     |
| DI-2           | БД/инварианты | **S1** | Класс: сторожа состояния объявлены `BEFORE UPDATE`, поэтому INSERT сразу в конечное состояние обходит их целиком (INV-115, FR-905, INV-076)                                                                               | договор рождается `active`, подписанным и зарегистрированным при 0 позиций сверки; протокол рождается с окном публикации 10 лет вместо 6 месяцев | fixed (договор, протокол); тендер - в круг 3                              |
| DI-3           | БД/торги      | **S1** | Победителем аукциона можно объявить не наибольшую ставку: проверялась принадлежность пары (заявка, сумма), но не максимальность                                                                                           | ставки 168 000 и 176 000, `UPDATE ... winner_amount=168000` → `UPDATE 1`, проигравший объявлен победителем                                       | fixed                                                                     |
| AC-4           | API           | **S1** | `Idempotency-Key` считается без тела и query-строки: другой запрос с тем же ключом получает 201 и **чужой** ответ, а сам не выполняется                                                                                   | загрузка `registration_certificate` под ключом первой загрузки вернула `tax_clearance` с `idempotent-replay: true`; документ не сохранен         | fixed                                                                     |
| FE-1           | фронтенд      | **S1** | Отказ загрузки чанка маршрута выбрасывает на тупиковый экран: «Повторить» повторяет тот же неудачный импорт и не восстанавливает никогда                                                                                  | `/auth/login`: SSR отдал форму, после гидратации `main` заменен на «Что-то пошло не так»; кнопка нажата дважды, экран тот же                     | fixed                                                                     |
| FE-4 / I18-1   | i18n          | **S1** | Служебный литерал `unknown_error` протекает в интерфейс: отфильтрован в 1 месте из 38                                                                                                                                     | `lib/auth.ts:63` возвращает голую строку; 73 вызова в 37 файлах отдают её прямо в `FormAlert`                                                    | fixed                                                                     |
| DI-1           | БД/сроки      | **S1** | Окно FR-303 (10 дней) и порядок FR-304 не защищены после объявления: срок и дата вскрытия меняются молча, без записи об изменении                                                                                         | на стенде лежит тендер с окном 1 день 23:30; `audit.log id=80` - UPDATE, `core.tender_amendments` пуст                                           | open (круг 3: правка сцеплена с посевом демо-тендера)                     |
| DI-4           | БД/WORM       | S2     | Протокол о результатах роль приложения переписывала и удаляла                                                                                                                                                             | `SET LOCAL ROLE tou_rent_app; DELETE FROM core.protocols` → `DELETE 1`                                                                           | fixed                                                                     |
| DI-5           | БД/заявки     | S2     | У поданной заявки подменялся заявитель, лот и тендер; журнал регистрации append-only после этого врёт                                                                                                                     | `UPDATE core.applications SET participant_id=...` → `UPDATE 1`, журнал не тронут                                                                 | fixed                                                                     |
| DI-6           | БД/сроки      | S2     | Заявка вставлялась после истечения срока: стоп INV-037 стоял только на журнале                                                                                                                                            | при дедлайне −10 дней `INSERT INTO core.applications` → `INSERT 0 1`, тот же INSERT в журнал → отказ                                             | fixed                                                                     |
| DI-9           | БД/WORM       | S2     | Материал досье переписывался: вид, источник и момент события не были в списке запретов, хотя `occurred_at` задаёт срок хранения                                                                                           | `UPDATE core.dossier_items SET kind='rewritten', occurred_at = −10 лет` → `UPDATE 2`                                                             | fixed                                                                     |
| DI-10          | БД/каскады    | S2     | `ON DELETE CASCADE` уносил запечатанное ценовое предложение и вложения мимо RLS INV-040                                                                                                                                   | удаление заявки → `price_proposals` 0 строк                                                                                                      | fixed                                                                     |
| RC-3           | часы          | S2     | `current_date` в пяти SQL-запросах обходит доменные часы (ADR-0005) и деловую дату по Алматы                                                                                                                              | при сдвиге часов боевой запрос даёт коэффициент 1.0000 вместо 9.9000; «действующая комиссия» - с истекшими полномочиями                          | fixed                                                                     |
| RC-4           | торги         | S2     | Идемпотентность ставки не привязана к аукциону и участнику, ранний возврат стоит до проверки допуска                                                                                                                      | `fetch_bid` фильтрует только по `b.id`; id ставок публичны в ленте                                                                               | fixed                                                                     |
| SEC-3          | доступ        | S2     | Смена и сброс пароля не завершают прочие сессии - угнанная cookie переживает смену пароля                                                                                                                                 | `auth.rs:483` перевыпускает только свою cookie; админский сброс не гасит ничего                                                                  | open (круг 3)                                                             |
| AC-1           | API           | S2     | Переполнение `numeric` (SQLSTATE 22003) уходит наружу как 500 на трёх маршрутах обычного пользователя                                                                                                                     | `area_m2=100000000.00` → 500 `internal`; граница 99999999.99 → 201                                                                               | fixed                                                                     |
| AC-2           | API           | S2     | Регистрация несуществующего договора → 500; 404 не документирован                                                                                                                                                         | `POST /contracts/000…000/register` → 500                                                                                                         | fixed                                                                     |
| AC-3           | API           | S2     | Перепутанные даты в форме тендера → 500 вместо внятного отказа                                                                                                                                                            | `opening_at` раньше `submission_deadline` → 500, лог: `violates check constraint`                                                                | fixed                                                                     |
| AC-5           | API           | S2     | Дата поступления взноса не ограничена: принимаются `0001-01-01` и будущее                                                                                                                                                 | обе проводки легли в книгу                                                                                                                       | fixed                                                                     |
| UW-1           | web           | S2     | Модуль валидации переменных окружения объявлен конвенцией, но не импортируется ниоткуда                                                                                                                                   | поиск по дереву - ни одного импорта `src/env`                                                                                                    | fixed                                                                     |
| UW-2           | web           | S2     | Реестр пользователей админа обрезан потолком 50 без листания и без признака усечения                                                                                                                                      | `next_after` сервер отдаёт, экран читает только `items`                                                                                          | open (круг 3)                                                             |
| UW-3           | web           | S2     | SMS-канал подтверждения реализован и переведён, но выбрать его в форме нельзя                                                                                                                                             | форма дважды шлёт константу `"email"`; три ключа Paraglide не используются                                                                       | wontfix-logged (нужен адрес шлюза - Q-020)                                |
| I18-2 … I18-14 | i18n          | S2     | Серверные строки и справочники проецируются только по-русски: ошибки api, письма с кодом, основания отклонения, категории особого порядка, назначения участков, реестры отчётности, названия объектов и лотов в кабинетах | `/en/auth/login` показывает русский текст ошибки; `GET /reports/contracts` отдаёт готовые русские заголовки колонок                              | частично fixed (I18-3, I18-4, I18-18, I18-19, I18-23), остальное - круг 3 |
| SEC-1          | доступ        | S3     | Полный контракт `/api/v1/openapi.json` отдаётся анониму, включая admin-маршруты                                                                                                                                           | 200, 232 883 байта, 154 пути                                                                                                                     | open (круг 3)                                                             |
| SEC-2          | доступ        | S3     | CSP в проде допускает `script-src 'unsafe-inline'` при готовой инфраструктуре nonce                                                                                                                                       | `infra/compose/Caddyfile:78`                                                                                                                     | open (круг 3)                                                             |
| AC-6           | контракт      | S3     | Контракт систематически не описывает коды, которые сервер отдаёт всегда: 401 у 179 операций из 182, 400/405/413/415/500 - ни у одной                                                                                      | `/applications/my` документирует только 200, живьём отдаёт 401                                                                                   | open (круг 3)                                                             |
| AC-7           | контракт      | S3     | 405 приходит с `code: "rule_violation"` без поля `rule`                                                                                                                                                                   | клиент проваливается на русский `detail`                                                                                                         | fixed                                                                     |
| AC-8           | контракт      | S3     | Контракт обещает, что участник видит свою цену до вскрытия; RLS этого не делает                                                                                                                                           | `GET /applications/my` → `price_amount: null` у собственной заявки                                                                               | open (круг 3, предметный)                                                 |
| AC-9           | API           | S3     | В `detail` ошибок 400/422 уходит сырой английский текст десериализатора, и веб рисует его как есть                                                                                                                        | `{"area_m2":null}` → `detail: "Failed to deserialize the JSON body…"`                                                                            | fixed                                                                     |
| RC-5           | торги         | S3     | `bid_step_percent` не ограничен сверху ни типом, ни CHECK, ни DTO                                                                                                                                                         | приняты `1000000` и 27 девяток                                                                                                                   | fixed                                                                     |
| DI-11          | БД            | S3     | Пустой `tstzrange` обходит EXCLUDE INV-DB-02: у объекта два действующих договора                                                                                                                                          | два INSERT с `'empty'::tstzrange` приняты                                                                                                        | fixed                                                                     |
| DI-13          | БД            | S3     | Проверки «не пустая строка» стоят только на казахских колонках                                                                                                                                                            | пустые `name`, `address`, `title`, `full_name` принимались                                                                                       | fixed                                                                     |
| DI-12          | БД/права      | S3     | Права на `refdata` противоречат тексту INVENTORY.md: DELETE на календаре и правка МРП на месте                                                                                                                            | `DELETE FROM refdata.holidays` → `DELETE 14`                                                                                                     | wontfix-logged (предметное решение, Q-021)                                |
| DI-14          | refdata       | S3     | Календарь и МРП покрывают только 2026 год, и ничто не проверяет попадание расчёта в покрытый период                                                                                                                       | `min/max(day)` = 2026-01-01 / 2026-12-16                                                                                                         | wontfix-logged (нужен текст Правил)                                       |
| DI-15          | гейты         | S3     | Уже закоммиченный файл миграции был переписан следующим коммитом; гейта на это нет                                                                                                                                        | `17038ea` добавил, `1312250` изменил тот же файл                                                                                                 | open (круг 3: гейт)                                                       |
| UW-4           | web           | S3     | Пункт «Отчётность» у админа всегда ведёт на «доступ закрыт»                                                                                                                                                               | `GET /reports` под админом → пустой список                                                                                                       | fixed                                                                     |
| UW-5           | web           | S3     | Девять из двенадцати страничных маршрутов упираются в потолок 50 без способа дойти до 51-й                                                                                                                                | листание есть только у `/objects`, `/tenders`, `/notifications`                                                                                  | open (круг 3)                                                             |
| UW-6           | посев         | S3     | Посев ставит тендерам статусы `trading`/`summed_up`/`contracted`, не создавая ни аукциона, ни протокола, ни договора                                                                                                      | `core.auctions`, `core.protocols`, `core.contracts` пусты                                                                                        | open (круг 3, сцеплено с DI-1)                                            |
| UW-7 / I18-16  | i18n          | S3     | Английские строки для скринридера в `components/ui/*`; 33 модуля не импортируются вовсе                                                                                                                                   | `breadcrumb.tsx:110` «More», `carousel.tsx:197,227`, `pagination.tsx:121`                                                                        | fixed                                                                     |
| UW-8 / I18-22  | i18n          | S3     | 12 ключей × 3 локали переведены под интерфейс, которого нет; два - дубликаты живых                                                                                                                                        | `dialog_close` дублирует заведённый в круге 1 `ui_close`                                                                                         | open (круг 3)                                                             |
| UW-9           | web           | S3     | Панель целостности аудита показывает сверку, снятую до наполнения журнала, и перепроверить её из интерфейса нечем                                                                                                         | `admin/audit/chain` → `entries: 0` при 91 записи в `audit.log`                                                                                   | open (круг 3)                                                             |
| UW-10          | docs          | S3     | Числа долга в документации разошлись с деревом (72/39 против 78/47; 697 ключей против 1090)                                                                                                                               | подсчёт вхождений `TODO-ENGINEER` по дереву                                                                                                      | open (круг 3)                                                             |
| FE-2           | фронтенд      | S2     | На публичной карточке тендера нет ни одного действия «подать заявку» или «войти, чтобы подать» - воронка обрывается                                                                                                       | все интерактивные элементы страницы: «← К реестру» и «Объявление (PDF)»                                                                          | open (круг 3)                                                             |
| FE-3 / I18-3   | i18n          | S2     | «Sign in with учетную запись университета» на `/en`, то же на `/kk`                                                                                                                                                       | серверная метка подставляется в локализованный шаблон                                                                                            | fixed                                                                     |
| FE-5           | фронтенд      | S3     | Карточка тендера одновременно «Прием заявок» и «Прием заявок закрыт»                                                                                                                                                      | бейдж читает статус из БД, блок срока - часы браузера                                                                                            | fixed                                                                     |
| FE-6           | фронтенд      | S3     | Юридически значимые сроки печатаются в зоне Asia/Almaty, но зона нигде не подписана                                                                                                                                       | `format.ts:4`, на странице «до 1 сентября 2026 г. в 21:00»                                                                                       | fixed                                                                     |
| I18-15         | i18n          | S3     | Гейт полноты переводов не смотрит на кабинеты: 10 казахских строк - дословные русские копии                                                                                                                               | `messages.test.ts:53-73` бежит по списку из 19 публичных файлов                                                                                  | open (круг 3)                                                             |

### Снятые гипотезы круга 2 (доказательство убило находку)

- Расхождение кодогена с контрактом: живой `/api/v1/openapi.json` совпал с
  закоммиченным `packages/api-client/openapi.json` **побайтово**, 182
  операции против 182. Дрейфа нет.
- Маршруты контракта, не подключённые к роутеру, и обработчики без
  маршрута: невозможны по построению - OpenAPI собирается из самого
  роутера (`crates/http/src/lib.rs:305`).
- Мертвые кнопки и ссылки в никуда: 21 подозрительная кнопка проверена
  поштучно - все триггеры `ConfirmAction`/`DialogTrigger`; все 48
  внутренних адресов существуют.
- Паритет локалей: ru/kk/en - по 1090 ключей, расхождений ноль в обе
  стороны; ключей, вызываемых из кода и отсутствующих в каталоге, - ноль.
- `todo!()`, `unimplemented!()`, обработчиков-заглушек, `#[ignore]`,
  `console.log` - ноль во всём дереве; `TODO` без токена - ноль.
- Повышение прав и IDOR: 86 мутирующих маршрутов из-под участника - все
  403; чужие заявки, файлы, уведомления, платежи - 403/404/409 без единой
  утечки. INV-040 держится двумя рубежами (RLS + шифрование).
- Перечисление пользователей, брутфорс пароля и кода подтверждения,
  фиксация сессии, CSRF, загрузка файлов, SQLi/XSS - ни одна проба не
  прошла (подробности - в отчёте `security-authz`).
