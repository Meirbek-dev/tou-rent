# BACKLOG (ТЗ § 9, арх. § 9)

Задачи выполняются строго по порядку; любой префикс списка - целостное демо.
Агент берет верхнюю невыполненную задачу (регламент А.2).
Контур 1 закрыт задачами T0–T17, контур 2 - задачами T18–T32, контур 3 - T33–T44;
контур 4 «производственная готовность» - задачи T45–T71 (ТЗ v2 § 5).

## Контур 1 - «демо завтра» (ТЗ § 9)

- [x] T0. Bootstrap базы монорепо: единый `apps/web` (TanStack Start + shadcn/ui base-nova инлайн),
      Rust-workspace (`crates/domain|db|http|testkit`, `apps/api|jobs`), `packages/api-client`,
      `specs/`, `infra/compose`, i18n kk/ru/en. _(выполнено агентной сессией 06.08.2026)_
- [x] T1. Достройка bootstrap: git-хуки (Vite+ вместо lefthook, ADR-0001), `.gitlab-ci.yml` гейты G1–G2, G5–G8
      (G3–G4, G9–G16 - заглушки, A-009), compose.dev проверен на PG 19beta2 (ADR-0002), пин Rust-зависимостей.
      _(выполнено агентной сессией 06.08.2026)_
- [x] T2. Схема БД v1 + миграции: ядро § 6 целиком (15 миграций), триггеры INV-021/037/040/063/066,
      RLS цен, EXCLUDE аренд (INV-DB-02), депозитная книга (INV-DB-05), audit hash-chain (INV-A01) + триггеры INV-AUDIT, `add_business_days` (SQL-половина G12), refdata seed (A-010).
      Проверено smoke-скриптом на живой PG 19beta2. _(выполнено агентной сессией 06.08.2026)_
- [x] T3. Домен: ставки (FR-201–204, golden-тесты insta ×11) + typestate статусов (единый макрос
      порождает методы переходов и `TRANSITIONS`), тест паритета Rust ↔ refdata-seed (INV-021).
      _(выполнено агентной сессией 06.08.2026)_
- [x] T4. Auth + роли: FR-1501 (Argon2id, сессии Redis, CSRF, авто-подтверждение email),
      FR-1503 (админ-эндпоинты ролей, аудит), политика INV-POL-01 + снапшот-матрица,
      seed-аккаунты ролей (`api seed`), подкоманды `api migrate|seed`. API проверен живым
      прогоном 15 сценариев (A-013: e2e-критерий переносится в T5/T6).
      _(выполнено агентной сессией 06.08.2026)_
- [x] T5. API «Объекты+Тендеры» + кодоген: FR-101 (CRUD + вычисляемый статус FR-103), FR-301–303
      (тендер с лотами, серверный снимок ставки, публикация с валидацией 10 дней триггером),
      rates/preview, цепочка utoipa → openapi.json → @tou/api-client (тест `openapi_sync` = G5),
      Swagger в dev (`--features swagger`). Живой прогон 20 сценариев.
      _(выполнено агентной сессией 06.08.2026)_
- [x] T6. Публичный портал (FR-1401, SSR, i18n): главная с актуальными объявлениями, реестр
      с фильтрами в URL (нативная GET-форма - работает без JS, NFR-04; фильтрация во фронте, A-017),
      карточка тендера (сроки, лоты со ставкой и взносом, заглушка документации до T7/T8),
      «как участвовать»; SSR через loader'ы + @tou/api-client, i18n kk/ru/en.
      Lighthouse accessibility 100 на карточке (критерий ≥90). _(выполнено агентной сессией 06.08.2026)_
- [x] T7. Кабинет организатора: вход (FR-1501 UI) + гард ролей (`ssr: false`, A-018), объекты (FR-101 UI),
      калькулятор (форма → типизированный `RateCalculationDto` с расшифровкой; справочник опций
      `GET /rates/options` из refdata), тендер с лотами (FR-301), даты черновика и переходы
      (publish/open-acceptance/cancel; отказ триггера FR-303/INV-021 показывается в UI),
      PDF объявления Typst (Прил. 1, A-019; у черновика - водяной знак). Демо-клик пройден живьем:
      объект → расчет → тендер → 409 при <10 днях → публикация → PDF. _(выполнено агентной сессией 06.08.2026)_
- [x] T8. Кабинет участника: заявка (FR-401–404): подача = заявка + запечатанная цена (Прил. 9) + запись журнала одной транзакцией (дедлайн стережет триггер INV-037, цены - RLS INV-040);
      мастер Прил. 2/11 (поля-приближение A-020), файлы в MinIO (object_store, доступ через api
      по FR-403: до вскрытия - только владелец), отзыв с фиксацией в журнале; кабинет секретаря:
      журнал Прил. 12 + заявки с запечатанными ценами. Критериальные тесты INV-037/INV-040
      в testkit (env-gated, A-021) - прогнаны против живой БД; e2e-прогон API+UI.
      Повторная подача после отзыва - Q-002. _(выполнено агентной сессией 06.08.2026)_
- [x] T9. Вскрытие и допуск (FR-501–503): вскрытие = переход в qualification + `opened_at`
      (CHECK не дает вскрыть раньше заседания - закреплено testkit-тестом; audit - триггер)
      с автосозданием заседания демо-комиссии (seed, A-022); цены/файлы открываются комиссии
      (RLS/FR-403 - проверено живьем); решения с основаниями п. 52 (FK INV-052) и голосами
      «за/против» членов (вносит секретарь, INV-055); протокол допуска - jsonb-снимок +
      Typst-PDF в MinIO, однократный (UNIQUE). Прогон UI: вскрытие → цена открылась →
      решение → протокол №Д-… (PDF с полями п. 55-приближения). _(выполнено агентной сессией 06.08.2026)_
- [x] T10. Уведомления in-app (FR-1301–1302): `core.notifications` + audit-триггер как
      доказательная база (закреплено testkit-тестом), enum типов событий в домене, REST
      (история/счетчик/прочтение) + SSE-стрим (in-process broadcast, A-023); событие FR-504
      «уведомить допущенных» - стартовая ставка = max цен допущенных по лоту (INV-062),
      торги = 3-й рабочий день (`refdata.add_business_days`), однократно (повтор - 409).
      Живой прогон: колокольчик участника получил событие за ~0,1 с (критерий ≤1 с),
      бейдж/поповер/история/прочтение - работают. _(выполнено агентной сессией 06.08.2026)_
- [x] T11. Аукцион демо-режим (FR-601–603, 606, WS-комната): комната лота со стартовой ставкой =
      максимум первоначальных предложений допущенных (INV-062) и шагом 5 % (домен, округление FR-204);
      server-authoritative таймер 60 минут с единственным продлением на 15 (INV-066, триггер), ставки
      допущенных ≥ максимум + шаг (INV-063, триггер) и идемпотентные по клиентскому id (NFR-05);
      лента и смены состояния - WS-комната (в dev сокет идет прямо в api, A-024); итог - победитель
      и второе место, привязанные к реальным ставкам (FR-606, триггер). Демо-таймер - `api --demo-timer 5m`.
      Property-тест монотонности максимума и единственности победителя (A-025), 3 testkit-теста против живой БД.
      Живой прогон: два участника торговались 55 000 → 79 750 (шаг 2 750), ставка ниже минимума и повторное
      продление отклонены, событие ленты дошло до второго браузера за ~0,24 с. _(выполнено агентной сессией 07.08.2026)_
- [x] T12. Протокол итогов (FR-701 PDF): jsonb-снимок + Typst-PDF в MinIO по данным завершенных торгов -
      состав комиссии, объекты и лоты, все заявки с первоначальными ценами и решениями (отклоненные с основаниями),
      победитель и второе место по каждому лоту (из core.auctions, т.е. из реальных ставок - FR-606), обязательства
      сторон (формулировки TODO-ENGINEER). Возможен только после завершения торгов по всем лотам (иначе 409),
      однократно (UNIQUE по виду); заседание итогов создается автоматически, срок оформления - 3 рабочих дня
      (refdata.add_business_days). Живой прогон: 409 при идущем лоте №2 → протокол №И-019FD645, PDF 49 КБ,
      повтор - 409. _(выполнено агентной сессией 07.08.2026)_
- [x] T13. Seed демо-данных + деплой (Прил. Б, deploy.sh, Caddy TLS): `api seed` наполняет стенд - шесть типовых
      площадей университета, тендеры во всех статусах жизненного цикла (портал) и «горячий» тендер § 9.1 с тремя
      поданными заявками и журналом, у которого прием закрыт, а время заседания наступило (демо начинается со
      вскрытия, A-028); коэффициенты - базовые опции `default` (A-027), все шаги идемпотентны. Деплой (NFR-10):
      образы api/web в infra/docker, прод-стенд за Caddy (TLS, /api без буферизации - SSE и WS), `deploy.sh`
      пулл → миграции → up -d → ожидание /healthz с окном заморозки по живой комнате торгов (обход DEPLOY_FORCE=1).
      Job публикации образов в Registry - отдельный MR: `.gitlab-ci.yml` защищен (A-029). Деплой на rent.tou.edu.kz
      не выполнялся - доступа к хосту у агента нет. _(выполнено агентной сессией 07.08.2026)_
- [x] T14. E2E Playwright демо-сценария § 9.1: два теста в apps/e2e - организатор (объект → расчет Прил. 4 →
      тендер → даты и публикация с FR-303 → карточка на портале и PDF объявления) и комиссия с торгами
      (вскрытие открывает цены INV-040 → двое допущены, один отклонен по п. 52 → протокол допуска PDF →
      уведомление FR-504 доходит до колокольчика → две сессии браузера торгуются вживую INV-063 → победитель
      FR-606 → протокол итогов PDF). Вторая половина идет на тендере от `api demo-tender` (внутри одного прогона
      свежий тендер до вскрытия не довести - FR-303 требует ≥10 дней, A-030). Прогон зеленый трижды подряд, ~40 с.
      Включение G11 в пайплайн - отдельный MR: .gitlab-ci.yml защищен. _(выполнено агентной сессией 07.08.2026)_
- [x] T15. Полнота переводов kk/en публичного портала: черновые формулировки вычитаны («допуск» в статусе -
      «қатысуға жіберу», «не состоялся» - «өткізілмеді», убран русизм «карточка», поправлены падежи; в en -
      «Declared failed», «Site viewing terms», «Announcement registry»). Полноту держит тест `messages.test.ts`:
      состав ключей и параметры подстановки совпадают с ru, строки портала не равны русским (кроме бренда,
      символа номера и заимствованного «Тендер {id}»), пустых строк нет - список публичных ключей вычисляется
      из исходников страниц. Прогон /kk и /en публичного портала проверен вживую. _(выполнено агентной сессией 07.08.2026)_
- [x] T16. Витрина свободных площадей (FR-102): SSR-страница без авторизации с карточками характеристик
      (вид, площадь, адрес, этаж/часть) и вычисляемым статусом объекта (FR-103); фильтры - статус, вид
      имущества, диапазон площади, поиск по названию и адресу - считает БД, состояние живет в URL и уходит
      нативной GET-формой (работает без JS, NFR-04). Проверено: фильтры отрабатывают на API и в SSR-разметке,
      страница читается в ru/kk/en. _(выполнено агентной сессией 07.08.2026)_
- [~] T17. Дашборд Grafana (RED + бизнес-метрики). **Отменено 09.08.2026 решением инженера (Q-018):**
  стек `grafana/otel-lgtm` и весь экспорт OTLP сняты с репозитория, вместе с ними удалены слой RED
  `http.server.request.duration` и бизнес-метрика `tou.tenders.count`. Задача была закрыта 07.08.2026
  и работала на стенде; отмена - не откат неудачной реализации, а смена решения по составу стека.
  Наблюдаемости бэкенда в системе больше нет (арх. v3 § 8). ТЗ v2 при этом продолжает требовать
  otel-lgtm в T49 и T70 - расхождение зафиксировано вопросом Q-018 и снимается ответом инженера:
  либо ТЗ правится, либо стек возвращается.

## Контур 2 - процессуальная полнота (арх. § 9; ТЗ § 4, колонка К = 2)

Состав контура: внешний провайдер идентичности с федерацией AD, тендерная комиссия целиком
(кворум, голосование, конфликт интересов), полный регламент торгов, двигатель обязательств,
депозитная книга, договорный конвейер с актами, признание несостоявшимся, шифрование цен,
гейты G9–G16 в error-режим. Приемка (ТЗ § 10в) - три e2e-сценария: «полный тендер до договора»,
«несостоявшийся → повтор», «победитель уклонился → № 2».

- [x] T18. Аутентификация через Zitadel (FR-1502, ADR-0003): OIDC authorization code + PKCE
      в api (`openidconnect`) - обмен кода и разбор `id_token` на сервере, браузер токенов
      не видит; `state`/`nonce`/PKCE-verifier в серверной сессии, аудитория сверх `client_id` -
      по явному списку. Субъект `(iss, sub)` линкуется к `core.users`: локальная запись с тем
      же email связывается, а не дублируется, смена email в AD пользователя не теряет.
      Роли из claim'а живут в `core.role_grants` источником `oidc` и снимаются вслед за
      провайдером, роли админа (`local`, FR-1503) синхронизация не трогает (A-033, A-035).
      Выход завершает сессию и у провайдера. Кнопка входа - обычная ссылка (без JS, NFR-04),
      появляется только при настроенном провайдере; без `OIDC_*` система работает как
      контур 1. Стенд: профиль compose `zitadel` + `infra/zitadel/provision.sh` (A-034).
      Живой прогон против Zitadel 4.16: вход демо-сотрудника → роли `secretary`+`commission`,
      отзыв роли в провайдере → снята только она; аудит связывания на месте.
      _(выполнено агентной сессией 07.08.2026)_
- [x] T19. Тендерная комиссия (FR-1101–1104): состав утверждается целиком с проверкой п. 9–11
      (один председатель, один заместитель, голосующих нечетное число ≥7, секретарь вне состава,
      срок полномочий ≤ года; A-038); заседание - отдельный шаг до вскрытия: явка, кворум ⅔ с
      председателем или заместителем, открытие; без открытого заседания вскрытие отклоняет
      триггер (A-037). Голосуют члены лично «за»/«против» с особым мнением, вердикт по заявке
      вычисляется из голосов - большинством присутствующих, при равенстве решает
      председательствующий (A-036). Декларации КОИ подает сам член; отвод фиксирует секретарь,
      отведенный теряет право голоса и доступ к материалам лота (RLS), его место занимает
      резервный. Протокол допуска получил кворум, явку, отводы и особые мнения (п. 55).
      Живой прогон: 4 из 7 → отказ по кворуму, 5 без председателя → отказ, 7 с
      председательствующим → заседание открыто; отведенный не голосует и не видит цены;
      равенство 3:3 разрешено голосом председательствующего. e2e § 9.1 обновлен и зелен.
      _(выполнено агентной сессией 07.08.2026)_
- [x] T20. Двигатель обязательств (FR-1701–1702): `domain::calendar` - рабочие дни РК, двойник
      SQL-функции `refdata.add_business_days`; паритет закреплен гейтом G12 (10⁴ случайных пар + границы вокруг праздников). Каталог сроков - закрытый enum `ObligationAction` со ссылкой
      на пункт Правил и ролью-исполнителем (п. 54, 57, 73, 75); срок ставит событие процесса
      (открытие заседания, протокол, завершение торгов) и закрывает исполнение, ручной отметки
      нет (A-039). Дни считаются по местному календарю (A-041). Воркер `apps/jobs` раз в интервал
      переводит просроченное в `overdue` и уведомляет носителей роли - однократно (A-040);
      дашборд «мои сроки» в кабинетах, справочник праздников правит админ.
      Живой прогон: открытие заседания → срок п. 54 на 12.08 (три рабочих дня от пятницы),
      протокол → срок закрыт и выставлен срок п. 57, просрочка → `overdue` + уведомление
      в колокольчике секретаря. _(выполнено агентной сессией 07.08.2026)_
- [x] T21. Взносы и депозитная книга (FR-405, FR-1001–1004): кабинет `finance` подтверждает
      поступление взноса вручную (сумма и дата по выписке - банк-интеграции нет), система
      проверяет равенство взносу лота и срок п. 23 (`paid_at + 2 р. дня <= первый этап`,
      тем же календарем, что и G12) и отказывает словами правила (A-042). Депозитная книга -
      двойная запись: направление проводки задано типом операции в домене, а CHECK'и и триггер
      БД держат debit XOR credit, неотрицательный баланс и неизменяемость (INV-DB-05).
      Возврат - на весь остаток с основанием из закрытого перечня п. 26 (`refdata.refund_reasons` + FK, паритет с enum домена; формулировки - Q-003, A-043); срок возврата 15 р. дней ставится
      отзывом заявки и отклонением участника, закрывается проводкой. Депозит по договору - пока
      только механика счета: его сроки идут вместе с договорами T24 (A-044).
      Живой прогон восьми сценариев: неполная сумма и опоздание отклонены, подтверждение перевело
      заявку в «взнос подтвержден», повтор отклонен, возврат обнулил счет, второй возврат отклонен.
      _(выполнено агентной сессией 07.08.2026)_
- [x] T22. Полный регламент торгов (FR-604, FR-605, FR-607): круг торгов - `domain::turn`
      (очередность, пропуск выбывших, замыкание круга, конец при одном активном) и
      `core.auction_participants` с тем же правилом в триггере: ставка вне очереди, ставка
      выбывшего и не явившегося отклоняются БД. Очередность - по номеру журнала регистрации
      (A-045), «пас» необратим и при одном оставшемся завершает торги без отметки досрочности
      (A-046). Неявка оглашает первоначальное предложение записью `announced`: шагу она не
      подчиняется, но планку не опускает (A-047); при неявке всех победитель определяется по
      максимальному первоначальному предложению без торгов (п. 71). Реконнект - догрузка ленты
      по курсору `seq` (`GET /auctions/{id}/bids?after_seq=`) и идемпотентность ставок по
      клиентскому id (NFR-05). UI комнаты показывает очередь, «ваш ход», кнопку «не повышаю»
      и отметку неявки у секретаря. Живой прогон: ставка вне очереди - 409, ставка по очереди
      передала ход, неявка огласила 48 000 и завершила торги (победитель 57 750, второе место -
      оглашенные 48 000); 4 testkit-теста против живой БД. _(выполнено агентной сессией 07.08.2026)_
- [x] T23. Несостоявшийся тендер (FR-801–802): `domain::failure` - четыре основания п. 81 и вывод
      следствия п. 82–83 из фактов (две неудачи подряд → Правление, единственная допущенная заявка →
      договор из одного источника, иначе повтор). Основание не выбирается человеком (A-048), а БД
      не принимает статус `failed` без основания из справочника `refdata.failure_grounds` и не
      позволяет записать следствие без основания. Признание ставит срок протокола (3 р. дня, п. 82),
      протокол - jsonb-снимок + Typst-PDF, однократно. Повторный тендер (A-049) - черновик со ссылкой
      `repeat_of` и копией лотов; при следствии «Правление» повтор отклоняется. Живой прогон: тендер
      с тремя заявками признать нельзя (409), пустой после дедлайна → основание п. 81.1 и следствие
      «повтор», протокол Н-019FDB82 (PDF 35 КБ), повтор создан и идемпотентен, у повторного тендера
      без заявок следствие уже «вопрос Правлению». 3 testkit-теста против живой БД.
      _(выполнено агентной сессией 07.08.2026)_
- [x] T24. Договорный конвейер (FR-901, FR-902, FR-905, INV-115): договор составляется из итогов
      торгов - ставка победителя, объект и срок переносятся снимком и дальше неизменяемы (тип без
      сеттеров + триггер `freeze_terms`, A-050). Шаги п. 110–115 хранятся отметками времени и идут
      строго по порядку (домен), сроки конвейера (15 → 10 → 7 → 2 → 2 р. дня) ставятся и закрываются
      событиями. INV-115 закреплен триггером: подпись наймодателя невозможна, пока не отмечен весь
      перечень сверки п. 113 (`refdata.checklist_items` + FK, A-051). Регистрация в журнале дает дату
      заключения и период найма, защищающий объект от пересечений (FR-905, INV-DB-02, A-052);
      печатная форма Прил. 5 - Typst-PDF в MinIO, скан подписанного экземпляра загружается без ЭЦП.
      Живой прогон: договор из торгов (60 500 ₸, PDF 31 КБ), шаг вне очереди - 409, подпись без сверки -
      «0/5 позиций», после отметки перечня подпись прошла, регистрация Д-2026/001. 4 testkit-теста
      против живой БД. Перечень п. 113 и формулировки Прил. 5 - Q-005.
      _(выполнено агентной сессией 07.08.2026)_
- [x] T25. Акты приема-передачи и возврата (FR-904, Прил. 7–8): `domain::act` - порядок актов
      (передавать можно зарегистрированный договор, возвращать - только переданный объект),
      те же правила триггером БД. Следствия наступают в БД: передача вводит договор в действие,
      задает начало начисления платы и периода найма датой акта (п. 122, 128–129), возврат закрывает
      договор и обрезает период - объект снова свободен (FR-103, A-053). Печатные формы Прил. 7 и 8 -
      Typst-PDF в MinIO, скан подписанного акта грузится без ЭЦП; акт удалить нельзя.
      Живой прогон: возврат до передачи - 409, акт передачи (PDF 23 КБ) → плата с 10.08 и объект сдан,
      повторная передача - 409, акт возврата → договор исполнен, период закрыт 15.09, объект свободен.
      4 testkit-теста против живой БД. _(выполнено агентной сессией 07.08.2026)_
- [x] T26. Уклонение победителя и № 2 (FR-903, FR-505): `domain::evasion` - закрытый перечень
      оснований п. 116 и вывод следствия (договор участнику № 2 либо основание п. 81.4), уклоняться
      можно только от переданного и неподписанного договора. Следствия в БД: взнос удерживается
      проводкой `hold` (п. 116), договор прекращается, договор с № 2 возможен лишь после уклонения
      победителя и лишь один раз (A-055). Сроки п. 117–118 (протокол 5 р. дней, уведомление
      следующий р. день) ставятся признанием и закрываются протоколом о победителе № 2 (Typst-PDF)
      вместе с уведомлением № 2. Реестр уклонистов бессрочен (A-054) и отклоняет их заявки
      автоматически основанием п. 52.4. Живой прогон: уклонение победителя → взнос 70 000 ₸ удержан,
      договор terminated, сроки к 10.08 и 14.08; договор с № 2 на его ставку 73 500 ₸; протокол
      У-… (PDF 41 КБ) + уведомление № 2, оба срока закрыты; уклонение № 2 → третий договор 409,
      основание п. 81.4 и признание несостоявшимся. 5 testkit-тестов против живой БД.
      _(выполнено агентной сессией 07.08.2026)_
- [x] T27. Изменение документации и отмена тендера (FR-304, FR-305, FR-1004):
      `domain::amendment` - окно правки п. 27 (не позже чем за 2 календарных дня до дедлайна,
      продление не менее чем на 10 календарных дней от публикации редакции) и условия отмены
      п. 78 (только с основанием и до заключения договора). Редакция документации - запись
      `core.tender_amendments` (append-only, дописывается только ключ печатной формы): БД сама
      продлевает срок приема и сдвигает вскрытие, снимок объявления кладется в MinIO, участники
      извещаются, срок извещения - п. 27. Отказ участника по п. 26.5 отзывает заявку со ссылкой
      на редакцию и ставит возврат взноса (FR-1004). Отмена тендера и отдельного лота - с
      основанием, взносы на возврат, извещение за 3 р. дня (п. 79); отмененный лот освобождает
      объект (FR-103, A-057). Живой прогон: редакция 2 (срок 27.08 → 11.09, PDF в MinIO,
      уведомление участнику), отказ по п. 26.5 → заявка отозвана и взнос у финблока, отмена лота
      без основания - 409, с основанием - объект свободен, отмена тендера - 409 на последующую
      редакцию. 5 testkit-тестов против живой БД. _(выполнено агентной сессией 07.08.2026)_
- [x] T28. Публикации, досье и публичные протоколы (FR-702, FR-703, FR-1402, FR-1602,
      INV-076): `domain::publication` - публичность как состояние со сроком (шесть месяцев,
      п. 76) и закрытый перечень видов материалов досье. Публикуется только сформированная
      печатная форма; момент публикации и снятие - юридические факты, БД не дает их переписать
      и вернуть протокол в публичный доступ (A-058). Срок п. 75 закрывается публикацией,
      участники получают копии (FR-703), снятие по истечении шести месяцев выполняет джоб
      (INV-076) - протокол остается в досье и в кабинете участника. Досье собирается триггерами
      в момент событий (заявка, протокол, публикация, редакция, договор, акт, уклонение),
      идемпотентно и без изъятия материалов; прошлые тендеры перенесены в досье миграцией.
      Выгрузка архивом - файлы по папкам разделов и манифест (FR-1602). Живой прогон:
      публикация протокола итогов → гость видит его и PDF (52 КБ), неопубликованный - 404,
      три копии участникам, срок п. 75 закрыт; джоб снял протокол с истекшим сроком; архив
      досье 121 КБ с манифестом. 5 testkit-тестов против живой БД.
      _(выполнено агентной сессией 07.08.2026)_
- [x] T29. Шифрование ценовых предложений at-rest (арх. § 6, п. 40): второй рубеж поверх RLS -
      цена хранится шифртекстом `pgp_sym_encrypt` на ключе тендера, производном от мастер-ключа
      приложения (HMAC-SHA256 по id тендера). Мастер-ключ в базе не хранится: приходит соединением
      из окружения (`PRICE_ENCRYPTION_KEY`, GUC `app.price_key`, A-060). Триггер стирает открытую
      цену при записи и отказывает без ключа; чтение - через `core.price_amount()`, без ключа и
      с чужим ключом возвращает NULL. Существующие цены дошифровывает шаг `api migrate`
      (122 записи на стенде). Живой прогон: заявка через API - в таблице 74 байта шифртекста,
      psql без ключа видит NULL, с ключом - 41 234,00 ₸, с подобранным ключом - NULL.
      4 testkit-теста против живой БД. _(выполнено агентной сессией 07.08.2026)_
- [x] T30. Почасовая аренда (FR-205): `domain::rates::calculate_hourly` - база 2 МРП за час,
      те же коэффициенты Прил. 4, нижняя граница п. 97 (понижающие коэффициенты минимум не
      пробивают, A-061); площадь в почасовой расчет не входит. Отдельный тип, а не поле в
      `RateCalculation` - golden-снапшоты (защищенный путь) не тронуты. Единица ставки лота -
      `core.rate_unit` (`monthly`/`hourly`) с объемом часов ровно у почасового лота; взнос
      почасового лота считает БД от объема (FR-206, A-062). Предпросмотр `POST /rates/preview-hourly`,
      выбор вида аренды в форме лота, единица ставки в объявлении Прил. 1. Живой прогон:
      предпросмотр 19 998 ₸/час (2×МРП), с социальным коэффициентом 0,5 - минимум удержал ставку,
      почасовой тендер создан (8 часов, взнос 159 984 ₸), PDF объявления 29 КБ.
      5 доменных и 4 testkit-теста. _(выполнено агентной сессией 07.08.2026)_
- [x] T31. Гейты G9–G16 в error-режим (арх. § 8): пайплайн переписан - заглушек
      `allow_failure` не осталось. G3 проверяет условие включения `sqlx prepare`, G4 линтует
      squawk'ом только миграции, добавленные MR (примененные - защищенный путь). G8/G12/G15/G16
      идут одним прогоном тестов против сервисной БД: добавлены тесты аудит-инвариантов
      (перечень INV-AUDIT читается из specs/INVENTORY.md) и трассировки INV. G9 - cargo-mutants
      по диффу домена, G10 - Schemathesis против поднятого api, G11 - Playwright, G13 -
      cargo-deny + bun audit + Trivy. Найденная уязвимость js-yaml устранена override'ом до
      4.3.1 (`bun audit` чист); восемь транзитивных RustSec-предупреждений (typst, openidconnect)
      внесены в `deny.toml` с обоснованием (A-063, A-064). Изменения защищенных путей приняты
      инженером 07.08.2026. Пайплайн на живом раннере не прогонялся - первый прогон выверяет
      версии образов сервисов и порог cargo-mutants (A-006, A-063).
      _(выполнено агентной сессией 07.08.2026)_
- [x] T32. E2E-приемка контура 2 (ТЗ § 10в): три сценария Playwright в `apps/e2e`
      (`loop2-acceptance.spec.ts`) против поднятого стенда. «Полный тендер до договора»:
      договор из итогов торгов → шаги п. 110–115 → INV-115 (подпись наймодателя без
      отмеченного перечня отклоняется) → регистрация в журнале → акт приема-передачи →
      досье собралось само и выгружается архивом. «Несостоявшийся → повтор»: основание
      п. 81.2 из фактов → протокол → черновик повтора со ссылкой `repeat_of` и лотами.
      «Победитель уклонился → № 2»: уклонение п. 116 → взнос удержан (счет обнулен) →
      протокол о победителе № 2 → договор со вторым местом на его ставку.
      Площадки готовят подкоманды `api demo-summed-up` и `api demo-single-application`
      (FR-303 не дает довести свежий тендер до торгов внутри прогона); у площадки
      с торгами свои объект и участники - INV-DB-02 и реестр уклонистов FR-505
      иначе сделали бы прогон одноразовым (A-065). Прогон: 5 тестов зелены (§ 9.1 + контур 2).
      По ходу приемки исправлены две находки: организатору был закрыт просмотр
      состояния п. 81 (403 - кнопка повтора не появлялась) и в кабинете не было
      кнопки составления договора по лоту. _(выполнено агентной сессией 07.08.2026)_

## Контур 3 - особый порядок и зрелость (арх. § 9; ТЗ § 4, колонка К = 3)

Раздел 12 Правил целиком (13 категорий, заключения, решения Правления, публикации
обоснований), земельные участки (раздел 14), WORM-архив, внешние каналы уведомлений,
допсоглашения и отчетность. Раздел 13 (госзакупки) - вне периметра (ТЗ § 1).

Инварианты контура нумеруются по пункту Правил, как и прежние (INV-021, INV-052,
INV-076): предложенные ниже ID регистрируются в `specs/INVENTORY.md` в момент, когда
задача берется в работу, - вместе с уровнем закрепления.

- [x] T33. Каркас особого порядка: категории и заявка (FR-1201, п. 87–88). INV-087 закреплен
      на трех уровнях: enum домена `SpecialCategory` без catch-all, справочник
      `refdata.special_categories` и FK заявки на него. Каждая категория объявляет данными
      требуемые документы (`refdata.special_category_documents`), срок проверки (FR-1202),
      льготную схему (`refdata.benefit_schemes`, FR-1205) и публикуемость (FR-1403) -
      значения заполняются по Правилам без правки кода. Заявка Прил. 3 в кабинете заявителя:
      подача, вложения по позициям перечня своей категории, печатная форма (Typst) и отзыв,
      пока решение не принято; порядок состояний п. 88–90 стережет триггер БД наравне
      с доменом, обе таблицы - в перечне INV-AUDIT. Наименования категорий и их требования
      из Правил недоступны (Q-009): категории заведены по номерам п. 87.1–87.13 - тем же
      способом, каким на них ссылается ТЗ («категории 4–5», FR-1203), подписи справочника
      помечены TODO-ENGINEER (A-066). Проверка подразделением и решение Правления - T34.
      Тесты: 6 доменных, 3 на печатную форму и 7 testkit против живой БД.
      _(выполнено агентной сессией 07.08.2026)_
- [x] T34. Проверка и решение Правления (FR-1202, п. 89–90, 97). Порядок стал состоянием
      системы: подача ставит срок проверки из категории (FR-1201, по умолчанию 15 календарных
      дней), заключение уполномоченного подразделения выносит заявку на рассмотрение Правления
      и открывает срок решения - 10 рабочих дней (FR-1702, FR-1701), решение из закрытого
      перечня «предоставить / отказать / направить в общий порядок» переводит заявку
      в терминальное состояние. INV-090 закреплен дважды: значение `BoardDecision` не построить
      без заключения, и триггер БД отклоняет решение по заявке без него. Заключение и решение
      неизменяемы (юридические факты, п. 97); протокол решения - Typst-PDF в бакете dossiers;
      заявитель извещается уведомлением с обоснованием (FR-1301). Обязательство получило
      четвертый предмет - заявку; отзыв заявки снимает открытые сроки. «Уполномоченное
      подразделение» - роль organizer, отдельной роли в ТЗ § 3 нет (A-068); публикация
      результата ≤5 рабочих дней (п. 97) идет задачей T39 (A-069).
      Тесты: 5 доменных, 2 на печатную форму и 6 testkit против живой БД.
      _(выполнено агентной сессией 07.08.2026)_
- [x] T35. Конкуренция заявок особого порядка (FR-1203, п. 86, 97). Правило конкуренции
      объявляет категория (данные, как и срок проверки): категориям 4–5 миграция ставит
      «общий порядок» - эти номера названы в самом ТЗ, инвестиционная категория помечается
      инженером вместе с ответом на Q-009. INV-086 закреплен типом `Competition::blocks`
      и триггером: пока за объект спорят несколько заявок, «предоставить» закрыто - по
      категориям общего порядка вовсе, по инвестиционной, если у конкурента существенно
      большая сумма; отказ и перевод в общий порядок Правлению доступны всегда, а при
      сопоставимых суммах (порог - данные категории, Q-013) выбор тоже за ним. Решение
      «направить в общий порядок» создает черновик тендера и уводит в него всю конкурирующую
      группу с тем же обоснованием; лоты добавляет организатор - ставку считает калькулятор
      Прил. 4, из заявки ее не вывести (A-070). Конкурентами считаются активные заявки той же
      категории на тот же объект (A-071). Тесты: 6 доменных и 5 testkit против живой БД.
      _(выполнено агентной сессией 07.08.2026)_
- [x] T36. Инвестиционные договоры (FR-1204, п. 91–94). Договор заключается по
      удовлетворенной заявке инвестиционной категории и живет в общей таблице договоров
      (объект, наниматель, ставка, INV-DB-02 - там же), а инвестиционная часть вынесена
      в `core.investment_contracts`. INV-091: комплект приложений п. 91 (смета, график работ,
      заключение оценщика, гарантия исполнения - закрытый перечень refdata + FK) проверяется
      перед подписанием триггером, как INV-115. INV-094: срок ≤7 лет закреплен типом
      `investment::Term` и CHECK. Приемка инвестиций - неизменяемые акты (п. 92) с печатной
      формой; продление на 3 года однократно при 100 % исполнения и объеме ≥30 млн ₸,
      пролонгация - от 100 млн ₸ решением Правления (п. 93), условия стережет триггер.
      Договор по инвесторской форме Прил. 6 - Typst-PDF (текст формы недоступен, TODO-ENGINEER
      и Q-014). Компенсация за неисполнение, повышенная плата и реестр ненадлежащих
      инвесторов из FR-1204 (последнее предложение, п. 94) остаются: величин в ТЗ нет,
      вопрос Q-014 (A-073). Тесты: 5 доменных, 3 на печатные формы и 8 testkit против живой БД.
      _(выполнено агентной сессией 07.08.2026)_
- [x] T37. Льготные схемы (FR-1205, п. 95–96, Прил. 4). Льгота - расписание платы по годам
      найма, а не скидка: первый год наниматель возмещает коммунальные расходы, со второго
      платит долю ставки Прил. 4 (доля - параметр схемы, по ТЗ 50 %). Социальному арендатору
      льгота приходит коэффициентом Ксоц внутри самого расчета (FR-201) - расписания у нее
      нет. Условия закреплены типом `benefit::Benefit::check` и триггером: INV-095 -
      согласование Ученого совета для оборудования в образовательном процессе, INV-096 -
      обучение спин-оффа не менее пяти кредитов в семестр; квота стажировок приходит данными
      справочника (величины в ТЗ нет - Q-010). Расписание считают домен и БД одной формулой,
      паритет проверяет тест. Панель льготы - в карточке договора особого порядка (A-074).
      Тесты: 7 доменных и 5 testkit против живой БД.
      _(выполнено агентной сессией 08.08.2026)_
- [x] T38. Досье решений и WORM-хранение (FR-1206, FR-1602, п. 97, 16.15, 42). Досье решения
      собирают те же триггеры, что и досье тендера (T28): заявка, ее документы, заключение
      подразделения, решение Правления с протоколом; точка входа `record_dossier_item`
      получила второй предмет - заявку, ключ идемпотентности у каждого свой. Публикации
      особого порядка лягут в то же досье задачей T39. INV-042 закреплен типом
      `DossierSubject::retention_years`, колонкой `retain_until` с триггером (срок считает БД
      от события, сократить его и отвязать файл нельзя, предмет досье не переписывается)
      и Object Lock бакета `dossiers` в режиме compliance на пять лет - предельный из двух
      сроков. Точка отсчета, режим блокировки и судьба материала после истечения срока -
      допущение A-075, вопрос Q-011 остается открытым.
      Тесты: 4 доменных и 5 testkit против живой БД.
      _(выполнено агентной сессией 08.08.2026)_
- [x] T39. Публикации особого порядка (FR-1403, п. 90, 92, 97). Публикация - отдельная запись
      `core.public_records` с закрытым перечнем видов (результат рассмотрения с обоснованием,
      обоснование ставки, акт приемки инвестиций): у трех материалов разные источники, и
      «опубликовать» для них значит выложить материал, а не открыть саму запись процесса.
      Механика - протокольная (FR-702, INV-076): публикуется сформированный документ,
      публикуемость объявляет категория (INV-087), срок публичного доступа считает БД
      (6 месяцев), снимает джоб, а факт публикации и снятия ложится в досье решения (T38).
      Публикация однократна, запись не удаляется. Срок публикации - 5 рабочих дней от решения
      (п. 97): обязательство `special_publish` ставится решением по публикуемой категории
      и закрывается самой публикацией. Обоснование ставки появилось на входе: ставку
      инвестиционного договора считает сервер по коэффициентам Прил. 4 и замораживает
      снимком - числом с экрана она больше не задается (A-076). Портал получил страницу
      `/special-orders`, открытую гостю. Тесты: 3 доменных и 6 testkit против живой БД.
      _(выполнено агентной сессией 08.08.2026)_
- [x] T40. Земельные участки (FR-1801, п. 104–107). Порядок раздела 14 собран целиком:
      характеристики участка (кадастровый номер, назначение из перечня п. 104, разрешенное
      использование, ожидаемый объем инвестиций) публикуются на портале - по неопубликованному
      участку заявку не принимает БД; заявка инвестора с проектом, объемом и сроком; решение
      Правления с обоснованием - неизменяемое и переводящее заявку в терминальное состояние;
      договор по удовлетворенной заявке в общей таблице договоров (INV-DB-02 действует),
      ставка - снимок расчета Прил. 4. INV-105 закреплен типом `land::Covenant` (закрытый
      перечень п. 107 в паритете со справочником) и двумя триггерами: условие нельзя снять
      или подменить, а без полного комплекта договор не переводится в подписание - тот же
      рубеж, что INV-091 и INV-115. Раздел 14 Правил недоступен: назначения и формулировки
      условий - TODO-ENGINEER и вопрос Q-016 (A-077).
      Тесты: 5 доменных и 6 testkit против живой БД.
      _(выполнено агентной сессией 08.08.2026)_
- [ ] T41. Внешние каналы уведомлений (FR-1303, п. 58). Email через корпоративный SMTP
      и Telegram-бот: расширение `core.notification_channel` и enum домена, сохранение MIME
      отправленного письма как доказательства (аналог п. 58), повторные попытки доставки,
      выбор каналов в профиле пользователя. Доставка не должна ронять транзакцию процесса:
      отправка - фоновая задача воркера (FR-1702-подобный проход).
- [x] T42. Допсоглашения (FR-906, п. 125). Допсоглашение - отдельная сущность с diff-контролем:
      `core.contract_amendments` (номер в рамках договора, основание, дата вступления) и
      `core.contract_amendment_changes` (поле, прежняя и новая редакция). Перечень полей
      договора закрыт типом `contract::ContractField` и разделен надвое: существенные условия
      (ставка, объект, срок, наниматель) защищены FR-901 и в справочник изменяемых не входят,
      остальное меняется допсоглашением. Правило стоит трижды: домен (`check_changes`), FK на
      закрытый перечень и триггер, отказывающий по имени поля, - если перечень когда-нибудь
      пополнят существенным условием, правило устоит. Соглашение заключается к
      зарегистрированному действующему договору (п. 126), неизменяемо (догружается только
      печатная форма Typst) и вместе с ней ложится в досье новым разделом «допсоглашения»
      (FR-1602). Состав изменяемых полей - данные справочника: п. 125 недоступен (Q-017, A-078).
      Тесты: 3 доменных и 5 testkit против живой БД.
      _(выполнено агентной сессией 08.08.2026)_
- [x] T43. Отчетность (арх. § 9 «отчетность»). Ответа на Q-012 нет, поэтому реестр сделан
      выборкой уже записанных фактов, а не формой отчета с реквизитами и подписями (A-079):
      три реестра - решений (особый порядок п. 90 и участки п. 106), договоров (только
      зарегистрированные, п. 126) и поступлений (приходные проводки книги, FR-1001) - за
      период, с колонками и строками из БД. Тип `report::Registry` держит перечень реестров
      и состав колонок, он же собирает выгрузку CSV (разделитель `;`, BOM - русская локаль
      Excel; экранирование кавычек и разделителей проверено тестами). Права разведены по
      предмету: решения - Правление и подразделение, договоры - организатор и финансы,
      поступления - только финансы. Выдуманных предметных констант нет: реестр показывает
      то, что уже есть в процессе, и потому всегда с ним сходится.
      Тесты: 4 доменных и 4 testkit против живой БД.
      _(выполнено агентной сессией 08.08.2026)_
- [x] T44. E2E-приемка контура 3 (ТЗ § 10в): два сценария Playwright в `apps/e2e`
      (`loop3-acceptance.spec.ts`) против поднятого стенда. «Особый порядок: инвестор»:
      заявка инвестиционной категории (FR-1201) → заключение подразделения (FR-1202, п. 89)
      → решение Правления (п. 90) → досье решения собралось само и выгружается архивом
      (FR-1206) → результат опубликован и виден гостю на портале (FR-1403) → инвестиционный
      договор со ставкой из снимка Прил. 4 и незакрытым комплектом п. 91 (FR-1204, INV-091).
      «Особый порядок: почасовая»: расчет почасовой ставки от 2 МРП/час (FR-205, п. 97) →
      заявка → заключение → решение → публикация и запись в досье → решение попало в реестр
      отчетности (T43). Площадку готовит `api demo-special-site`: свой объект на прогон
      (INV-DB-02, как в T32) и помеченная инвестиционная категория (Q-013).
      Гейт G16 дополнен трассировкой требований: `fr_traceability` берет из ТЗ все FR
      контура 3 и требует теста с привязкой к ID; единственное исключение - FR-1303
      (T41 пропущена по решению заказчика) - объявлено списком в самом гейте.
      По ходу приемки исправлена находка: список объектов в форме заявки страничный,
      и объект за пределами первой страницы заявителю был недоступен - добавлен поиск.
      Прогон: 7 тестов зелены (§ 9.1 + контур 2 + контур 3).
      _(выполнено агентной сессией 08.08.2026)_

## Контур 4 - производственная готовность (ТЗ v2 § 5)

Состав и обоснование - [ТЗ v2.0](../docs/tou-rent-tz-v2.md). Порядок внутри
групп значим; ключевое отличие от контуров 1–3 в том, что группа A (гейты,
секреты, деплой, бэкап) требует раннера GitLab, доступа к хосту и ключей -
без инженера эти задачи не закрываются.

### A - доказательность

- [ ] T45. Живой пайплайн GitLab: подключить раннер, прогнать G1–G16, снять A-006/A-063. **Нужен инженер.**
- [ ] T46. Настоящий G3: включить `sqlx/macros`, перевести запросы на `query_as!`, `.sqlx` в репозиторий.
      Разблокировано в T73: 125 запросов больше не склеиваются `format!`, их текст существует
      к моменту раскрытия макросов (`macro_rules!` + `concat!`), а `query_as!` иначе их и не увидел бы.
      Сборка в контейнере проверена и работает - VS Build Tools на дев-хосте не нужны (A-003).
      Цепочка проверена целиком пилотом 09.08.2026: `sqlx/macros` включается, `query_scalar!`
      типизируется по живой схеме PG 19beta2, `cargo sqlx prepare --workspace` пишет `.sqlx`.
      Пилот откачен намеренно - остаток задачи требует решения инженера: 1. `query_as!` не использует `FromRow`, он собирает структуру сам. У десятка записей поля
      доменных типов (`Role`, `PublicRecordKind`, `DossierKind`), которые сейчас получаются
      через `.parse()` в рукописном `FromRow`. Чтобы их принял макрос, домену нужны
      `sqlx::Type`/`Decode` - то есть sqlx в `crates/domain`, чего расслоение (арх. v3 § 5.2)
      не предполагает. Альтернатива - оставить `query!` и собирать структуры руками,
      потеряв половину выгоды. 2. Включение `macros` делает `.sqlx` обязательным для сборки образа (в нем БД нет),
      а значит перегенерацию - шагом всякой правки запроса. Это контракт сборки,
      его стоит вводить сознательно.
      **Нужно решение инженера по обоим пунктам.**
- [ ] T47. Секреты: SOPS+age, гейт gitleaks, ротация ключей. **Нужны ключи age.**
- [ ] T48. Публикация образов в Registry + Trivy по образам (A-029). **Нужен инженер.**
- [ ] T49. Деплой на rent.tou.edu.kz по NFR-10 + смоук + NTP. **Нужен доступ к хосту.** Сам
      прод-контур уже поднимался целиком локально (T75), поэтому здесь остается непроверенным
      именно то, что требует хоста и реестра: `compose pull`, откат на `.deployed-tag`, окно
      заморозки под живыми торгами, ACME-сертификат на реальном домене и NTP.
- [x] T50. Запрет межслойных импортов фронта: правила `no-restricted-imports` в конфиге Vite+ вместо dependency-cruiser.
- [ ] T51. pgBackRest + WAL в RustFS + `restore.sh` + учебное восстановление (NFR-06, NFR-13). **Нужен хост.**

### B - незакрытые требования

- [x] T52. Ссылка на запись торгов (FR-306, FR-1903, п. 72): `PUT /tenders/{id}/recording` под правом `AuctionManage`, поле в панели торгов после итогов, правка раньше отклоняется условием запроса. Заодно закрыта утечка ссылок на конференцию и запись анонимному читателю портала. Тесты: 3 testkit против живой БД. _(выполнено агентной сессией 08.08.2026)_
- [x] T53. Кабинет админа: справочники и роли (FR-1901, FR-1902, FR-202, FR-1701). МРП по годам, версии коэффициентов Прил. 4 с датой вступления, календарь; правки справочников идут в аудит (в том числе через `audit.record_natural_key()` для таблиц с естественными ключами). Перечень опций Прил. 4 закрыт каталогом `refdata.rate_options`: отказ 422 вместо ошибки целостности, в форме - выбор пары из списка. Найдено: у роли приложения нет `DELETE` на справочники, поэтому мутации получили варианты `*_on` и тест идет в откатываемой транзакции. Тесты: 4 testkit. _(выполнено агентной сессией 08.08.2026)_
- [x] T54. Регистрация участника в интерфейсе (FR-1504, FR-1501): форма со входом сразу после регистрации, ссылка со страницы входа. Живой прогон: 201 → 200 → кабинет. Сценарий Playwright - вместе с T68. _(выполнено агентной сессией 08.08.2026)_
- [ ] T41. Внешние каналы уведомлений (FR-1303, п. 58). **Требуется подтверждение заказчика: пропуск отменяется или фиксируется решением.**
- [ ] T55. Льготные схемы: значения квоты и доли второго года (Q-010). **Нужен ответ заказчика.**
- [ ] T56. Инвест-договоры: компенсация, повышенная плата, реестр ненадлежащих (Q-014). **Нужен ответ заказчика.**
- [ ] T57. Федерация Zitadel ↔ AD университета (FR-1502). **Нужен администратор домена.**
- [x] T63. Задел под подпись (ТЗ § 2): `domain::signing` (`SignatureStatus`, `SigningProvider`, заглушка `PaperSigning`) и `signature_status` у договоров и актов, выводимый из наличия скана триггером; паритет типа и триггера под тестом. Протоколы поля не получили - их скан система не хранит. Тесты: 4 доменных и 4 testkit. _(выполнено агентной сессией 08.08.2026)_

### C - качества системы

- [x] T58. SSE и WS через Redis pub/sub (NFR-12, A-023/A-024): работа при двух экземплярах api.
      Реализовано вместе с ADR-0005-сессией: `http::realtime` (шина на `fred`, каналы `tou:notify`
      и `tou:auction`), `AppState::attach_realtime`, подписка поднимается в `apps/api`. Путь доставки
      остался один - локальный broadcast, поэтому дублей нет. Задача оставалась незакрытой в списке;
      статус сверен 09.08.2026.
- [ ] T59. Нагрузочное подтверждение NFR-02 на прод-подобном стенде. **Нужен стенд.**
- [x] T60. Гейт доступности (axe-core, G17) по публичным маршрутам (NFR-15).
      `apps/e2e/tests/accessibility.spec.ts`: восемь публичных маршрутов × две темы, 0 нарушений
      уровней A и AA, плюс карточка опубликованного тендера и проверка работы без JS (NFR-04).
      Гоняется вместе с остальными сценариями в job `e2e`. Статус сверен 09.08.2026.
- [x] T64. Персональные данные в логах (NFR-07, NFR-16): `domain::redacted::Redacted<T>`, сведения заявителя за ним в слое данных, рукописный `Debug` у `UserRecord`. _(выполнено агентной сессией 08.08.2026)_
- [x] T71. ПДн во внешней аналитике (D-13): очистка событий Sentry и PostHog, выключение при пустых ключах.
      `telemetry.mjs` (общие правила очистки для обоих входов), `instrument.server.mjs`
      (`dataCollection` выключен по категориям, `beforeSend`/`beforeBreadcrumb`), провайдер PostHog
      (автозахват и запись сессии выключены, `sanitize_properties`). Оба сервиса не поднимаются
      при пустом ключе.
      Найдено при сверке 09.08.2026 и исправлено в T73: в прод-образе Sentry не запускался вовсе -
      `web.Dockerfile` копировал `instrument.server.mjs`, но стартовал сервер без него, так что вся
      очистка охраняла то, чего нет. Теперь и образ, и скрипт `start` идут через `bun --preload`.

### D - документы и решения

- [x] T61. Прод-версия PostgreSQL: решение принято 09.08.2026 - прод стартует на **19beta2**,
      той же версии, что дев-стенд (ADR-0002). Ждать GA и переезжать на 18 ради промежутка
      не будут: расхождение версий между стендом и продом само по себе риск, а весь набор
      инвариантов (триггеры, RLS, EXCLUDE, hash-цепочка аудита) проверяется тестами testkit
      именно на 19beta2. Compose обоих стендов уже пиньован на `postgres:19beta2` (T70).
      Что это значит на практике: до GA обновление минорной версии - плановая операция
      с бэкапом (T51), а не автоматика.
- [x] T62. Расслоение: `ports`/`application` объявлены пилотом, правило «новый сценарий с >1 вызывающим или >1 адаптером - через порты» (арх. v3 § 5.2).
- [x] T65. Архитектура v3 (`docs/tou-rent-architecture-v3.md`) описывает фактическую систему; реестр дрейфа ТЗ v2 § 3 закрыт.
- [ ] T66. Закрытие Q-001…Q-017 и рассмотрение A-001…A-08x; заполнение справочников. **Нужен первоисточник и ответы заказчика.**
- [ ] T67. Сверка 12 печатных форм Typst с Прил. 1–12. **Нужен первоисточник.**
- [x] T70. Пиньованные теги инфраструктурных образов вместо `latest` в прод- и дев-стендах: версии сняты с образов, проверенных на стенде, каждый тег проверен в реестре. _(выполнено агентной сессией 09.08.2026)_
- [ ] T72. Гигиена сборочного кеша. Том `tou-rent-target` дорос до 101 ГБ: 68 ГБ в `debug/deps` (8424 файла) и 32 ГБ инкрементальных данных. Cargo старые артефакты не вытесняет сам, а прогоны `check`/`clippy`/`test` с разными наборами фич множат их. Ручные команды заведены (`vp run cache:size` / `cache:sweep` / `cache:clean`, README § «Быстрый старт»), разовая чистка 09.08.2026 освободила ~105 ГБ - остается автоматическая отсечка по возрасту (`cargo-sweep --time 7`) в job пайплайна, чтобы кеш не рос между ручными вмешательствами.

- [x] T73. Критический разбор репозитория и правка находок, не требующих заказчика (09.08.2026).
      Разбор шел не по списку задач, а по коду; закрыто девять расхождений кода с уже принятыми
      решениями:
      **прод** - Sentry не запускался в образе web (см. T71); api и jobs умирали по сигналу вместе
      с открытыми запросами, в том числе с комнатой торгов, ради которой `deploy.sh` держит окно
      заморозки; у CSRF-cookie не было `Secure`, хотя у сессионной был;
      **устойчивость** - у запроса не было потолка времени (NFR-02), у входа - ограничения попыток
      (перебор не оставлял даже следа в логе), у Caddy - CSP и Permissions-Policy;
      **время** - миграция T68 объявила `core.now()` единственным источником, но перевела только
      триггеры: 72 запроса слоя данных, пять отметок в печатных формах и фикстуры тестов остались
      на часах процесса; гейт G2 при этом не ловил `time::OffsetDateTime::now_utc` - строка
      в `clippy.toml` добавлена, и без нее находка повторилась бы;
      **доказательность** - без `TESTKIT_DATABASE_URL` 32 интеграционных файла возвращали управление
      из тела теста, и `cargo test` рапортовал успех, не проверив ни одного инварианта в триггерах,
      RLS и CHECK; в пайплайне это теперь ошибка. Корневые `bun run lint|typecheck|format` были
      пустышками: turbo искал скрипты, которых у пакетов нет, и возвращал ноль.
      Сверены и закрыты по факту T58, T60, T61, T71.
      Вторым заходом закрыты и две отложенные находки:
      **запросы** - 125 склеек `format!` переведены в литералы времени компиляции, пять мест,
      собиравших SQL из значений (имя колонки, число дней, минуты продления, хвост `SET`),
      переведены на параметры и на ветвление целыми запросами. Тексты сверены байт в байт
      со старой версией, T46 разблокирован (см. его запись);
      **выборки** - из 76 функций, возвращающих `Vec` без `LIMIT`, ограничены восемнадцать,
      у которых нет естественной границы; остальные ограничены самими Правилами. Потолок
      (`MAX_ROWS`) не молчит: достижение пишется в лог. Три прохода воркера получили размер
      пачки (`BATCH_ROWS`) с `FOR UPDATE SKIP LOCKED`. Форма пакетного отбора проверена
      на 250 просроченных обязательствах, исполнимость всех ограниченных выборок - тестом
      `bounded_lists` против живой БД.
      Не тронуто намеренно: неиспользуемые зависимости (`apalis`, `schemars`, `proptest`,
      `testcontainers-modules`, `deadpool-redis`, `tower-http`) и 46 незадействованных компонентов
      `ui/` - это задел, а не мусор.
      _(выполнено агентной сессией 09.08.2026)_

- [x] T74. Критический разбор инфраструктуры и правка найденного (10.08.2026). Разбор шел по
      `infra/` и `.gitlab-ci.yml`; закрыто восемь расхождений замысла с тем, что в файлах:
      **прод не поднимался** - проба здоровья `web` ходила через `wget`, которого в рантайм-образе
      `oven/bun` нет (в финальном слое только ca-certificates), Caddy же стартует лишь по здоровому
      `web`, то есть стенд встал бы целиком, а сообщение об отказе указывало бы на api. Проба
      переведена на сам bun и на отдельный маршрут `/healthz`, а не на главную: SSR главной ходит
      в api, и недоступность api уводила бы `web` в unhealthy следом;
      **сессии** - Redis работал с выключенными RDB и AOF при смонтированном томе, поэтому всякий
      перезапуск, включая штатный деплой, разлогинивал всех разом и обнулял счетчики попыток входа;
      окно заморозки берегло комнату торгов от обрыва, а сессию участника рядом с ней - ничто.
      Включен AOF, добавлен пароль (сеть стенда общая), `api` ждет Redis здоровым, а не запущенным;
      **гейты по диффу** - в пайплайне не было ни `workflow`-правил, ни `rules`, поэтому GitLab
      заводил только пайплайны веток, `CI_MERGE_REQUEST_TARGET_BRANCH_NAME` не заполнялась никогда,
      и G4 (squawk), G2/SQL (`core.now()`) и G9 (мутации по диффу) молча уходили в ветку «в MR
      ничего нового»: зеленые, не посмотрев ни на одну миграцию. База сравнения вынесена в
      `infra/ci/diff-base.sh` - он либо дает ref, либо падает; полный прогон мутаций теперь
      действительно ночной, а не на каждый push. Там же выяснилось, что `cargo install squawk-cli`
      не работает вовсе: крейт называется `squawk`;
      **кеш и пины CI** - кешировался `.cargo/registry`, тогда как cargo в образе ходит
      в `/usr/local/cargo`: путь ехал пустым, реестр выкачивался в каждом задании, а четыре
      инструмента собирались из исходников, три из них без версии. `CARGO_HOME` переведен
      в каталог проекта, кеши разделены по назначению, версии инструментов и образов
      (gitleaks, alpine, redis, trivy, schemathesis, bun) пиньованы - тот же довод, что в T70;
      **прод-стенд** - у контейнеров не было ни потолков памяти, ни ограничения привилегий:
      одна машина держит и приложение, и базу, и утечка в `jobs` выселяла бы Postgres.
      Добавлены `mem_limit` (A-099), `no-new-privileges`, `cap_drop: [ALL]` с точечными
      исключениями для Caddy и Postgres, `shm_size` базе и выключенная консоль RustFS;
      **деплой** - при неудачной проверке здоровья сломанная версия оставалась обслуживать
      трафик. Появился откат на последний успешный тег (`.deployed-tag`); схему БД откат
      не трогает, и скрипт говорит об этом прямо (A-100). Проверяются оба контейнера
      трафика, а не один api;
      **дев-стенд** - `dossiers` заводился в compliance-режиме на пять лет, то есть пробные
      файлы досье не удалялись с машины разработчика ничем, кроме пересоздания тома. На стенде
      governance на сутки, в проде без изменений (A-098).
      Проверено: `/healthz` отдает 200 живым сервером, `vp check` (247 файлов), `vp test`
      (45 тестов), YAML обоих compose и пайплайна разбирается, слияние якорей проверено дампом.
      Не проверено вживую: сам пайплайн (нет раннера, T45). Прод-стенд проверен следующей
      задачей - T75, и живой запуск нашел три отказа, включая неподнимающийся Redis ровно
      из-за пары правок этой задачи.
      _(выполнено агентной сессией 10.08.2026)_

- [x] T75. Живой запуск прод-контура на машине разработчика (10.08.2026). Проверка того,
      что T74 правил вслепую: прод-compose собран и поднят целиком под podman
      (`TOU_DOMAIN=localhost`, Caddy выдает сертификат внутренним CA вместо похода
      в Let's Encrypt). Найдено три отказа, каждый из которых валил стенд или врал о его
      состоянии:
      **образ `web` не собирался вовсе** - `bun install --frozen-lockfile` отбивал установку,
      потому что рабочие области заданы шаблоном `apps/*`, а Dockerfile копирует манифесты
      только `web` и `api-client`: внутри контекста сборки bun видел другой набор рабочих
      областей, чем записан в `bun.lock`. Сам `bun.lock` в порядке - на полном дереве
      `--frozen-lockfile` проходит. Добавлен `COPY apps/e2e/package.json`; на рантайм-образ
      это не влияет, в него едет только `.output`. Сломано с a1e4413, где появился `apps/e2e`;
      **Redis падал на первом же старте** - `Can't open or create append-only dir`. Штатный
      вход образа снимает права до `redis` только при наличии SETUID и SETGID, а их снял
      `cap_drop: [ALL]` из T74: сервер оставался root'ом, а root без DAC_OVERRIDE не пишет
      в каталог, принадлежащий `redis` (999). До T74 это было незаметно - с выключенными RDB
      и AOF сервер в том не писал ничего. Задан `user: "999:999"` (A-101): две правки T74,
      верные по отдельности, вместе давали неподнимающийся стенд;
      **`jobs` вечно unhealthy** - воркер берет образ api вместе с его `HEALTHCHECK`
      на `/api/v1/healthz`, портов при этом не слушая. Проба отключена (A-102); попутно
      выяснилось, что комментарий в compose про игнорирование `HEALTHCHECK` в OCI-формате
      к этой сборке не относится - проба из образа применяется.
      Проверено вживую на поднятом стенде: HTTP→HTTPS (308), `/api/v1/healthz` через Caddy 200,
      `/` на `web` 200, шесть заголовков безопасности на месте и `Server` вычищен; 44 миграции
      накачены (51 таблица в `core`), повторный `api-migrate` идемпотентен; Redis требует пароль
      и переживает перезапуск с сохранением ключа - то, ради чего T74 включал AOF; `dossiers`
      создан с Object Lock COMPLIANCE на 5 лет, ключ приложения кладет и читает, но получает
      AccessDenied на `DeleteObject` и `DeleteBucket`, сохраняя удаление в `public` (INV-042,
      подтверждает A-094 в прод-режиме); обе пробы здоровья из `deploy.sh` и его запрос окна
      заморозки отрабатывают. Стенд и тома после проверки снесены.
      Не проверено: `deploy.sh` целиком - он начинается с `compose pull`, а реестра за локально
      собранными образами нет, поэтому пулл, откат на `.deployed-tag` и окно заморозки под живыми
      торгами остаются за T49; применение `mem_limit` на rootless podman (A-099).
      _(выполнено агентной сессией 10.08.2026)_

### E - сквозная приемка

- [ ] T68. Управляемое время в e2e: сценарии контуров 2–3 без вставки состояния подкомандами `demo-*`.
- [ ] T69. Приемка контура 4 (ТЗ v2 § 8).
