# Портал публикует объявления о тендерах, и их индексация - часть публичности
# процедуры (FR-1401): реестр, объявления, земельные участки и публикации
# особого порядка открыты для поисковых систем намеренно.
#
# Закрыты две ветки. `/app/` - личные кабинеты: там нет ничего, что имеет
# смысл вне сессии, а ссылки с идентификаторами заявок и договоров в индексе
# не нужны никому. `/api/` - машинный интерфейс: его ответы не страницы.
User-agent: *
Disallow: /app/
Disallow: /api/
Allow: /
